# Implement the following plan
Follow the `Instructions` to implement the `Plan` then `Report` the completed work.

## Instructions
- Read the plan, think hard about the plan and implement the plan.
- THINK about which agents to delegate work to.
- Follow the plan step by step. Don't skip steps.
- After implementation, verify your code compiles/builds without errors.

### Add Session Context
- Add what was done and what was edited in a concise manner to `.claude/context/session-context.md`

## Plan
$ARGUMENTS

## Report
- Summarize the work you've just done in a concise bullet point list.
- Report the files and total lines changed with `git diff --stat`
