export { confirmAlert, evaluateLevel, computeScore, DEFAULT_CONFIRMATION_CONFIG } from './engine';
export type {
  ConfirmationConfig,
  ConfirmationLevel,
  ConfirmationResult,
  TimeframeResult,
} from './types';
